import ReactDOM from "react-dom/client";
import One from "./One";
const rootElement = document.getElementById('root');
const rootRef = ReactDOM.createRoot(rootElement);
rootRef.render(<div><One /></div>);